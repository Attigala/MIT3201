package com.masters.backend.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.masters.backend.model.ExemptFromPayment;

public interface ExemptFromPaymentRepository extends JpaRepository<ExemptFromPayment, Long>{

}
